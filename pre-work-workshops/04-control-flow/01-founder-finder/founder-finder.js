let name = "David";
// let name = 'Nimit';
// let name = 'Someone else';

let found;

// YOUR CODE BELOW
if (name == "David" || found == "Nimit") {
  found = true;
} else if (name == "Someone else") {
  found = false;
} else {
  found = "not boolean";
}
